interface:
  display_name: "Gdp"
  short_description: "Repo-specific patterns and workflows for gdp"
  default_prompt: "Use the gdp repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
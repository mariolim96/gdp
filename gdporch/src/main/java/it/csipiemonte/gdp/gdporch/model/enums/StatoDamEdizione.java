package it.csipiemonte.gdp.gdporch.model.enums;

/**
 * Enum per lo stato di caricamento dell'edizione verso il DAM (GDP_LOG_EDIZIONE.STATO).
 */
public enum StatoDamEdizione {
    SUBMITTED,
    FAILED,
    COMPLETED
}
